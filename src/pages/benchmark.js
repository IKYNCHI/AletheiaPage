import React from 'react';
import ScrollToTop from '../components/ScrollToTop';

const benchmark = () => {
  return <div>
    <ScrollToTop/>
      <h1>benchmark</h1>
  </div>;
};

export default benchmark;
